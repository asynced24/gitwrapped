# GitWrapped Environment Variables
# Copy this file to .env.local for local development

# GitHub Personal Access Token (optional — unauthenticated = 60 req/hr, authenticated = 5000 req/hr)
# Recommended: Use a Fine-Grained Personal Access Token with NO permissions (public data only)
# Generate at: https://github.com/settings/personal-access-tokens/new
# DO NOT use classic tokens with the 'repo' scope — that grants write access to all your repositories
GITHUB_TOKEN=your_github_token_here
